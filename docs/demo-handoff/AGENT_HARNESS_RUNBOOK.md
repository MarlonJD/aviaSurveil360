# Legacy Agent Harness Runbook Pointer

This former runbook has been retired to remove duplicated instructions.

Use the canonical [agent harness index](../agent-harness/index.md), its
[capability registry](../agent-harness/registry.md), and the
[verification matrix](../agent-harness/verification-matrix.md).

Historical execution plans may still mention this path; this compatibility
pointer preserves those references without maintaining a second runbook.
