<!doctype html>
<html lang="en">
  <body>
    <h1>{{.Title}}</h1>
    <p>{{.Summary}}</p>
    {{if .InternalContext}}<section><h2>Internal CAA Note</h2><p>{{.InternalContext}}</p></section>{{end}}
    {{if .RelatedRecordID}}<p>Authorized record: {{.RelatedRecordType}} {{.RelatedRecordID}}</p>{{end}}
    <p>Sign in to AviaSurveil360 to view the authoritative record.</p>
  </body>
</html>
